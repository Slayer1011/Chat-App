<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="8dp">
    <RelativeLayout
        android:id="@+id/relativeLayout1"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentEnd="true"
        android:background="@drawable/message_text_style"
        android:layout_marginEnd="7dp"
        android:focusable="true"
        android:clickable="true"
        android:foreground="?android:attr/selectableItemBackground">

        <TextView
            android:id="@+id/rightMessage"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignParentTop="true"
            android:layout_marginStart="6dp"
            android:layout_marginTop="6dp"
            android:layout_marginEnd="6dp"
            android:layout_marginBottom="6dp"
            android:text=""
            android:textColor="@color/white"
            android:textSize="15dp"
            android:textStyle="bold" />
    </RelativeLayout>

    <ImageView
        android:id="@+id/secondThick"
        android:layout_width="20dp"
        android:layout_height="15dp"
        android:layout_below="@id/relativeLayout1"
        android:layout_toLeftOf="@id/rightTimeStamp"
        android:background="@drawable/baseline_done_all"
        android:visibility="gone" />
    <ImageView
        android:id="@+id/firstThick"
        android:layout_width="20dp"
        android:layout_height="15dp"
        android:layout_below="@id/relativeLayout1"
        android:layout_toLeftOf="@id/rightTimeStamp"
        android:background="@drawable/baseline_done"
        android:visibility="gone" />
    <TextView
        android:id="@+id/rightTimeStamp"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="10:00 AM"
        android:layout_below="@id/relativeLayout1"
        android:layout_alignEnd="@id/relativeLayout1"
        android:textSize="10sp" />
</RelativeLayout>